# 不配置 DATABASE_URL 时，应用会以内存演示模式启动：
# 目录数据来自种子常量，自选与测算记录仅存在于进程内存中，重启即清空。
# 配置后自动切换到 Postgres 持久化模式，首次启动会自动灌入种子数据。
#
# 本地 Docker 起库（见 docker-compose.yml）：
# DATABASE_URL=postgres://tradelens:tradelens@localhost:5432/tradelens
DATABASE_URL=
